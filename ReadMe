The application is quite straightforward. After running the project, append "/models" to the end of your port number in your web address bar.
From here you can add, delete, edit, and view Contact entires. The firstName and lastName fields are set to a max of 20 characters, the street to 100, and city is set
to 50. The email field is also set to validate a correct email format.
